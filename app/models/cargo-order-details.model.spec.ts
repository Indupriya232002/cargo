import { CargoOrderDetails } from './cargo-order-details.model';

describe('CargoOrderDetails', () => {
  it('should create an instance', () => {
    expect(new CargoOrderDetails()).toBeTruthy();
  });
});
